services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: rater
      POSTGRES_PASSWORD: rater_dev
      POSTGRES_DB: rater_development
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
