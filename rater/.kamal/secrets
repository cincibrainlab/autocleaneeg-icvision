# Kamal 2.x secrets configuration
# Secrets are available under: registry/password, env/secret, builder/secrets, accessories/*/env/secret
# Pull from password manager, ENV, or files. DO NOT ENTER RAW CREDENTIALS HERE!

# Example: 1Password integration
# SECRETS=$(kamal secrets fetch --adapter 1password --account your-account --from Vault/Item KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY POSTGRES_PASSWORD)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})
# RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})
# POSTGRES_PASSWORD=$(kamal secrets extract POSTGRES_PASSWORD ${SECRETS})

# Example: Rails credentials
# KAMAL_REGISTRY_PASSWORD=$(rails credentials:fetch kamal.registry_password)

# Example: GitHub token for private repos
# GITHUB_TOKEN=$(gh config get -h github.com oauth_token)

# Example: Registry password from ENV
# KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Rails master key (never commit config/master.key to git!)
RAILS_MASTER_KEY=$(cat config/master.key)

# PostgreSQL password (generate a strong password for production)
# For production, use a password manager or generate with:
# openssl rand -base64 32
POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
